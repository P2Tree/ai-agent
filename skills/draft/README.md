# Draft

开发中的 skill，尚未通过质量审核。

通过审核后将移动到正式桶目录。

## bash-to-zsh-converter `[自动]`

把 bash 脚本转成 zsh 兼容语法。内置 10 项常见不兼容问题清单，每项带严重度、示例和修复方案，逐个对照改就行。

**什么时候用：**有 bash 脚本需要在 zsh 环境里跑时。

**触发方式：**你说"把 bash 转成 zsh"或明确要求 shell 兼容性转换时自动激活。

## symbol-refactor `[自动]`

在 Rust 项目里重命名、删除、修改符号：先扫全量引用，改完后验证编译、同步测试、同步文档，确保不遗漏。

**什么时候用：**需要重命名、删除或修改 Rust 里的符号时。

**触发方式：**你说"重命名这个 struct""删掉这个 trait"等涉及 Rust 符号变更的操作时，agent 自动激活。
